apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  
  labels:
    app: jupyter-spark-claim1
  name: jupyter-spark-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
