apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  
  labels:
    app: jupyter-spark-claim0
  name: jupyter-spark-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
